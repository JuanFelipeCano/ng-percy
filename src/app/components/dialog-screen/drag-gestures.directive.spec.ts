import { DragGesturesDirective } from './drag-gestures.directive';

describe('DragGesturesDirective', () => {
  it('should create an instance', () => {
    const directive = new DragGesturesDirective();
    expect(directive).toBeTruthy();
  });
});
